# Variables used for all hosts that are part of the "textbox" (vagrant) group

# --- users and groups

genericusers_groups:
    - name: "admin"
    - name: "vagrant"

# Used by Ansibles.generics-users

genericusers_users:
  - name: vagrant
    pass: satrF5fwANvrQ
    comment: 'vagrant'
    uid: 1000
    shell: /bin/bash
    groups:
    - vagrant
    - admin
    append: no
    ssh_keys:
      - "{{ lookup('file', '/Users/chrisseto/.ssh/id_rsa.pub') }}"
  - name: cseto
    pass: satrF5fwANvrQ
    comment: 'vagrant'
    uid: 2000
    shell: /bin/bash
    groups:
    - sudo
    - admin
    - staff
    append: yes
    # Set your key here
    # You can use a file like so:
    # "{{ lookup('file', '/Users/you/.ssh/id_rsa.pub') }}"
    ssh_keys:
      - "{{ lookup('file', '/Users/chrisseto/.ssh/id_rsa.pub') }}"


# --- csf
csf_allowed_ips:
  - CHANGEME
csf_ui_user: CHANGEME
csf_ui_password: CHANGEME


# --- rkhunter

# Email address to receive rkhunter warnings, "" for no emails
rkhunter_mail_on_warnings: ""

# Once rkhunter has been installed and updated the first time, set this to "no"
rkhunter_propupd: yes

rabbitmq_url: "amqp://archiver:archiver@192.168.111.112//"
